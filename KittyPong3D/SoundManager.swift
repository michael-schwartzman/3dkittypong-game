import Foundation

// Simple sound manager with no audio functionality to avoid crashes
class SoundManager {
    
    init() {
        print("SoundManager initialized - no audio")
    }
    
    func playHitSound() {
        // No audio for now
    }
    
    func playWallHitSound() {
        // No audio for now
    }
    
    func playScoreSound(isPlayer: Bool) {
        // No audio for now
    }
    
    func playStartSound() {
        // No audio for now
    }
}