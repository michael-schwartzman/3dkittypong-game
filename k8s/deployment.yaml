apiVersion: apps/v1
kind: Deployment
metadata:
  name: 3dkittypong-game
  labels:
    app: 3dkittypong-game
spec:
  replicas: 2
  selector:
    matchLabels:
      app: 3dkittypong-game
  template:
    metadata:
      labels:
        app: 3dkittypong-game
    spec:
      containers:
      - name: 3dkittypong-game
        image: kittypongacr.azurecr.io/3dkittypong-game:IMAGE_TAG_PLACEHOLDER
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 20