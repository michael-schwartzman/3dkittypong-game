apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: 3dkittypong-game-ingress
  labels:
    app: 3dkittypong-game
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
  - hosts:
    - your-domain.com
    secretName: 3dkittypong-game-tls
  rules:
  - host: your-domain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: 3dkittypong-game-service
            port:
              number: 80