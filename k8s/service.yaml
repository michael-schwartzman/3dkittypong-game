apiVersion: v1
kind: Service
metadata:
  name: kittypong-game-service
  labels:
    app: kittypong-game
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: kittypong-game